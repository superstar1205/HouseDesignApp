import { MyMathOprPipe } from './my-math-opr.pipe';

describe('MyMathOprPipe', () => {
  it('create an instance', () => {
    const pipe = new MyMathOprPipe();
    expect(pipe).toBeTruthy();
  });
});
