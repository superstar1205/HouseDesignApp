import { FilterArrPipe } from './filter-arr.pipe';

describe('FilterArrPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrPipe();
    expect(pipe).toBeTruthy();
  });
});
