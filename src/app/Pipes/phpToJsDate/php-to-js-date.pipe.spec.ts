import { PhpToJsDatePipe } from './php-to-js-date.pipe';

describe('PhpToJsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PhpToJsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
