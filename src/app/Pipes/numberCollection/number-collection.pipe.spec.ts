import { NumberCollectionPipe } from './number-collection.pipe';

describe('NumberCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
